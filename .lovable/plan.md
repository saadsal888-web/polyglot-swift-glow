

# فتح جميع الوحدات بالكامل

## التغيير المطلوب

ازالة نظام القفل من صفحة الخريطة بحيث يقدر المستخدم يدخل أي وحدة بدون قيود.

## التعديلات في `src/pages/LessonsHub.tsx`

1. **ازالة متغير `isLocked`** -- خليه دايماً `false`
2. **ازالة أيقونة القفل** -- كل الوحدات تعرض أيقونتها الطبيعية
3. **ازالة `opacity-40`** -- كل البطاقات تظهر بوضوح كامل
4. **ازالة `disabled={isLocked}`** -- كل الأزرار تكون قابلة للضغط
5. **تحديث لون الدائرة** -- الوحدات اللي ما اتكملت تاخذ لون المستوى بدل اللون الرمادي

### الأسطر المتأثرة

- سطر 191: حذف `const isLocked = ...`
- سطر 194-198: تعديل `circleColor` ليعطي لون المستوى للوحدات الغير مكتملة
- سطر 227-229: ازالة شرط `!isLocked` والـ `disabled`
- سطر 236-242: ازالة شرط عرض أيقونة القفل
- سطر 245-254: ازالة `disabled` و `opacity-40`

